import { PlaceholderScreen } from '../../src/screens/PlaceholderScreen';

export default function MoveRoute() {
  return (
    <PlaceholderScreen
      eyebrow="Move Engine"
      title="Move"
      description="GPS routes, walking, running, cycling, hiking, weather-aware suggestions, and location-aware activity recommendations feed the daily command center."
    />
  );
}
